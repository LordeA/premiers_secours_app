import 'package:flutter/material.dart';

class CprTimerScreen extends StatelessWidget {
  const CprTimerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Minuteur RCP')),
      body: const Center(
        child: Text('Minuteur RCP — à construire au jour 6-7'),
      ),
    );
  }
}